"""Rule engine adapters for country-specific decoding.

Submodules
----------
* ``engine``   — provider-agnostic decode interface
* ``opa``      — Open Policy Agent HTTP adapter
* ``local``    — in-process fallback using Python business rules
"""

from __future__ import annotations
