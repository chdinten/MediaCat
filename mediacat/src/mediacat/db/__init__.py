"""Database layer — SQLAlchemy 2.0 models, engine factory, repositories.

Submodules
----------
* ``enums``  — PostgreSQL-backed enum types
* ``base``   — declarative base and common mixins
* ``models`` — ORM models
* ``engine`` — async engine and session factory
"""

from __future__ import annotations
