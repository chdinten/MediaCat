# Trivy container image scanner configuration.
# Usage: trivy image mediacat:dev --config .trivy.yaml
severity:
  - CRITICAL
  - HIGH
exit-code: 1
ignore-unfixed: true
scanners:
  - vuln
  - secret
  - misconfig
